---
description: Set up or troubleshoot LuckPerms permissions, groups, tracks, contexts and prefixes.
argument-hint: "[what you want to grant / the problem]"
allowed-tools: Read, Bash, Glob, Grep, Skill
---

Use the **permissions-helper** skill for: `$ARGUMENTS`

Establish the rank ladder (groups + a track) before assigning nodes; scope per-world/per-server
perks with contexts; set group weights for clean prefix resolution. Give the exact `/lp …`
commands to run, in order. If a plugin's node names are uncertain, use **learn-plugin-docs** first
— do not invent nodes.
