# Docs-learner cache (regenerated on demand)
skills/_cache/

# Node
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Packed skill zips (produced by scripts/pack-skill.mjs)
dist/
*.skill.zip

# Secrets — never commit a real Minecraft proxy secret
forwarding.secret

# Maintainer-only / local dev — never publish to GitHub.
# Build brief, original research, and the local-only dev docs (how-it-was-built notes,
# name rationale, decisions log, publishing guide).
/goal.md
/research.md
/.dev-docs/

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
